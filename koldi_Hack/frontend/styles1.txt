body{
margin:0;
font-family:'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
background:#f4f6f8;
color:#1f2937;
}

/* ===== NAVBAR ===== */

.navbar{
height:110px;
background:rgba(255,255,255,.98);
backdrop-filter:blur(16px);
display:grid;
grid-template-columns:1fr auto 1fr;
align-items:center;
padding:0 65px;
box-shadow:0 2px 16px rgba(0,0,0,.04);
position:sticky;
top:0;
z-index:1000;
transition:.3s;
border-bottom:1px solid rgba(0,0,0,.05);
}

.nav-left{
display:flex;
align-items:center;
}

.nav-left img{
height:72px;
width:auto;
object-fit:contain;
transition:.25s;
}

.nav-center{
justify-self:center;
text-align:center;
}

.nav-center h1{
margin:0;
font-size:58px;
font-weight:800;
color:#17324d;
letter-spacing:-2px;
line-height:1;
}

.nav-center p{
margin-top:8px;
font-size:14px;
font-weight:600;
letter-spacing:2px;
text-transform:uppercase;
color:#6b7280;
}

.nav-right{
display:flex;
justify-content:flex-end;
align-items:center;
gap:48px;
}

.nav-right a{
text-decoration:none;
color:#17324d;
font-size:18px;
font-weight:700;
transition:.3s;
position:relative;
padding-bottom:6px;
}

.nav-right a:hover{
color:#0d6e8a;
transform:translateY(-1px);
}

.nav-right a::after{
content:"";
position:absolute;
left:0;
bottom:0;
width:0;
height:3px;
background:#0d6e8a;
transition:.3s;
border-radius:10px;
}

.nav-right a:hover::after{
width:100%;
}

/* ===== HERO ===== */

.hero{
height:720px;
background:url('assets/banner.jpg');
background-size:cover;
background-position:center 35%;
background-repeat:no-repeat;
display:flex;
align-items:center;
position:relative;
overflow:hidden;
}

.overlay{
display:none;
}

.hero-content{
position:relative;
z-index:2;
max-width:760px;
padding-left:85px;
padding-top:40px;
color:white;
background:linear-gradient(90deg, rgba(0,0,0,.28) 0%, rgba(0,0,0,.16) 45%, rgba(0,0,0,0) 100%);
padding:70px 80px 70px 60px;
border-radius:0;
box-shadow:none;
backdrop-filter:none;
}

.hero h1{
font-size:88px;
font-weight:780;
line-height:.95;
letter-spacing:-3px;
margin:0 0 22px 0;
color:white;
text-shadow:0 4px 14px rgba(0,0,0,.20);
}

.hero p{
font-size:28px;
line-height:1.7;
font-weight:400;
max-width:650px;
color:rgba(255,255,255,.94);
text-shadow:0 2px 8px rgba(0,0,0,.18);
}

.hero::after{
content:"";
position:absolute;
left:0;
right:0;
bottom:0;
height:140px;
background:linear-gradient(transparent, #f4f6f8);
}

/* ===== KPIS ===== */

.kpis{
display:grid;
grid-template-columns:repeat(3,1fr);
gap:30px;
padding:40px 60px;
}

.card{
background:white;
padding:30px;
border-radius:20px;
box-shadow:0 4px 20px rgba(0,0,0,.08);
}

.card h2{
font-size:40px;
color:#002B45;
}

/* ===== DASHBOARD ===== */

.dashboard{
padding:20px 60px;
}

.panel{
background:white;
padding:30px;
border-radius:20px;
box-shadow:0 4px 20px rgba(0,0,0,.06);
margin-bottom:30px;
}

/* ═══════════════════════════════════════════════════════════════════════════════
   CHAT COMPONENTS - PROFESSIONAL REDESIGN
   ═══════════════════════════════════════════════════════════════════════════════ */

/* ===== FLOATING CHAT BUTTON ===== */

#chat-toggle{
position:fixed;
right:32px;
bottom:32px;
width:68px;
height:68px;
border:none;
border-radius:50%;
background:linear-gradient(135deg, #17324d 0%, #24395c 100%);
display:flex;
align-items:center;
justify-content:center;
cursor:pointer;
z-index:9999;
box-shadow:
    0 8px 24px rgba(23, 50, 77, 0.35),
    0 4px 12px rgba(0, 0, 0, 0.15);
transition:all .3s cubic-bezier(0.4, 0, 0.2, 1);
font-size:32px;
}

#chat-toggle:hover{
transform:scale(1.12) translateY(-2px);
box-shadow:
    0 12px 32px rgba(23, 50, 77, 0.45),
    0 6px 16px rgba(0, 0, 0, 0.2);
}

#chat-toggle:active{
transform:scale(1.05);
}

/* Optional: Add a pulsing animation for attention */
@keyframes pulse-ring{
0%{
    box-shadow:
        0 0 0 0 rgba(23, 50, 77, 0.6),
        0 8px 24px rgba(23, 50, 77, 0.35);
}
50%{
    box-shadow:
        0 0 0 12px rgba(23, 50, 77, 0),
        0 8px 24px rgba(23, 50, 77, 0.35);
}
100%{
    box-shadow:
        0 0 0 0 rgba(23, 50, 77, 0),
        0 8px 24px rgba(23, 50, 77, 0.35);
}
}

#chat-toggle.pulse{
animation:pulse-ring 2s infinite;
}

/* ===== CHAT WINDOW ===== */

#chat-popup{
position:fixed;
right:32px;
bottom:118px;
width:440px;
height:680px;
background:#ffffff;
border-radius:24px;
overflow:hidden;
display:none;
flex-direction:column;
z-index:9998;
box-shadow:
    0 20px 60px rgba(0, 0, 0, 0.25),
    0 4px 16px rgba(0, 0, 0, 0.12),
    inset 0 0 0 1px rgba(255, 255, 255, 0.1);
animation:slideUp 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes slideUp{
from{
    opacity:0;
    transform:translateY(20px) scale(0.95);
}
to{
    opacity:1;
    transform:translateY(0) scale(1);
}
}

/* ===== CHAT HEADER - PROFESSIONAL GRADIENT ===== */

.chat-header{
background:linear-gradient(135deg, #17324d 0%, #24395c 50%, #2d4a6f 100%);
padding:24px 28px;
color:white;
position:relative;
box-shadow:0 4px 16px rgba(0, 0, 0, 0.1);
}

.chat-header::before{
content:"";
position:absolute;
top:0;
left:0;
right:0;
height:100%;
background:linear-gradient(180deg, rgba(255,255,255,0.05) 0%, transparent 100%);
pointer-events:none;
}

.chat-header h3{
margin:0;
font-size:22px;
font-weight:700;
letter-spacing:-0.3px;
position:relative;
z-index:1;
}

.chat-header::after{
content:"● Online";
display:block;
font-size:13px;
margin-top:6px;
color:#7ee787;
font-weight:500;
letter-spacing:0.3px;
position:relative;
z-index:1;
}

/* ===== MESSAGE AREA ===== */

#chat-messages{
flex:1;
padding:24px 20px;
overflow-y:auto;
display:flex;
flex-direction:column;
gap:16px;
background:linear-gradient(180deg, #f8f9fb 0%, #f3f5f8 100%);
}

/* Custom scrollbar */
#chat-messages::-webkit-scrollbar{
width:6px;
}

#chat-messages::-webkit-scrollbar-track{
background:transparent;
}

#chat-messages::-webkit-scrollbar-thumb{
background:rgba(0,0,0,.12);
border-radius:10px;
}

#chat-messages::-webkit-scrollbar-thumb:hover{
background:rgba(0,0,0,.2);
}

/* ===== AI MESSAGE BUBBLE - REDESIGNED ===== */

.ai-msg{
background:white;
padding:16px 20px;
border-radius:18px 18px 18px 6px;
max-width:85%;
font-size:15px;
line-height:1.6;
color:#2c3e50;
box-shadow:
    0 2px 8px rgba(0, 0, 0, 0.06),
    0 1px 4px rgba(0, 0, 0, 0.04);
align-self:flex-start;
word-break:break-word;
white-space:pre-wrap;
position:relative;
border:1px solid rgba(0, 0, 0, 0.04);
animation:messageSlideIn 0.3s ease-out;
}

/* AI avatar indicator */
.ai-msg::before{
content:"🤖";
position:absolute;
left:-34px;
top:0;
font-size:24px;
background:white;
width:28px;
height:28px;
border-radius:50%;
display:flex;
align-items:center;
justify-content:center;
box-shadow:0 2px 8px rgba(0, 0, 0, 0.1);
}

@keyframes messageSlideIn{
from{
    opacity:0;
    transform:translateY(8px);
}
to{
    opacity:1;
    transform:translateY(0);
}
}

/* ===== USER MESSAGE BUBBLE - REDESIGNED ===== */

.user-msg{
background:linear-gradient(135deg, #17324d 0%, #24395c 100%);
color:white;
padding:14px 20px;
border-radius:18px 18px 6px 18px;
max-width:82%;
font-size:15px;
font-weight:500;
line-height:1.55;
align-self:flex-end;
box-shadow:
    0 4px 12px rgba(23, 50, 77, 0.3),
    0 2px 6px rgba(0, 0, 0, 0.15);
word-break:break-word;
white-space:pre-wrap;
margin-left:auto;
animation:messageSlideIn 0.3s ease-out;
}

/* ===== INPUT AREA - MODERN DESIGN ===== */

.chat-input-area{
padding:20px;
background:white;
border-top:1px solid #e8ecef;
box-shadow:0 -4px 16px rgba(0, 0, 0, 0.04);
}

/* ===== INPUT WRAPPER - FLOATING STYLE ===== */

.input-wrapper{
display:flex;
align-items:flex-end;
gap:12px;
background:#f8f9fb;
padding:10px 12px;
border-radius:24px;
border:2px solid #e5e8ec;
transition:all .25s ease;
}

.input-wrapper:focus-within{
border-color:#17324d;
background:white;
box-shadow:
    0 0 0 4px rgba(23, 50, 77, 0.08),
    0 4px 12px rgba(0, 0, 0, 0.08);
}

/* ===== TEXTAREA - CLEAN & MODERN ===== */

#question{
flex:1;
border:none;
outline:none;
resize:none;
background:transparent;
font-size:15px;
line-height:1.5;
min-height:24px;
max-height:120px;
padding:8px 8px;
font-family:'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
color:#2c3e50;
overflow-y:auto;
}

#question::placeholder{
color:#9ca3af;
}

/* Custom scrollbar for textarea */
#question::-webkit-scrollbar{
width:4px;
}

#question::-webkit-scrollbar-track{
background:transparent;
}

#question::-webkit-scrollbar-thumb{
background:rgba(0,0,0,.1);
border-radius:10px;
}

/* ===== SEND BUTTON - ELEVATED DESIGN ===== */

.send-btn{
width:42px;
height:42px;
border:none;
border-radius:50%;
background:linear-gradient(135deg, #17324d 0%, #24395c 100%);
color:white;
font-size:20px;
cursor:pointer;
display:flex;
align-items:center;
justify-content:center;
flex-shrink:0;
box-shadow:
    0 4px 12px rgba(23, 50, 77, 0.3),
    0 2px 6px rgba(0, 0, 0, 0.15);
transition:all .25s cubic-bezier(0.4, 0, 0.2, 1);
position:relative;
}

.send-btn:hover{
transform:scale(1.1) translateY(-1px);
box-shadow:
    0 6px 16px rgba(23, 50, 77, 0.4),
    0 3px 8px rgba(0, 0, 0, 0.2);
}

.send-btn:active{
transform:scale(1.05);
}

.send-btn:disabled{
opacity:0.5;
cursor:not-allowed;
}

/* ===== THINKING INDICATOR ===== */

.ai-msg.thinking{
opacity:0.7;
}

.ai-msg.thinking::after{
content:"●●●";
display:inline-block;
animation:thinkingDots 1.4s infinite;
margin-left:6px;
letter-spacing:2px;
}

@keyframes thinkingDots{
0%, 20%{
    opacity:0.3;
}
40%{
    opacity:1;
}
60%, 100%{
    opacity:0.3;
}
}

/* ===== TYPING INDICATOR - ANIMATED DOTS ===== */

.typing-indicator{
display:flex;
gap:4px;
padding:12px 16px;
align-self:flex-start;
background:white;
border-radius:18px 18px 18px 6px;
box-shadow:0 2px 8px rgba(0, 0, 0, 0.06);
}

.typing-indicator span{
width:8px;
height:8px;
border-radius:50%;
background:#17324d;
animation:typingBounce 1.4s infinite ease-in-out;
}

.typing-indicator span:nth-child(1){
animation-delay:0s;
}

.typing-indicator span:nth-child(2){
animation-delay:0.2s;
}

.typing-indicator span:nth-child(3){
animation-delay:0.4s;
}

@keyframes typingBounce{
0%, 60%, 100%{
    transform:translateY(0);
    opacity:0.5;
}
30%{
    transform:translateY(-10px);
    opacity:1;
}
}

/* ===== WELCOME MESSAGE STYLING ===== */

.ai-msg.welcome{
background:linear-gradient(135deg, #f0f4f8 0%, #e8eef5 100%);
border:1px solid #d1dce8;
font-weight:500;
}

/* ===== TIMESTAMP (Optional) ===== */

.message-time{
font-size:11px;
color:#9ca3af;
margin-top:4px;
text-align:right;
}

/* ===== MOBILE RESPONSIVE ===== */

@media (max-width: 768px){
#chat-popup{
    width:calc(100vw - 32px);
    height:calc(100vh - 150px);
    right:16px;
    bottom:90px;
}

#chat-toggle{
    right:16px;
    bottom:16px;
    width:60px;
    height:60px;
    font-size:28px;
}
}

/* ===== ACCESSIBILITY ===== */

.send-btn:focus-visible,
#chat-toggle:focus-visible{
outline:3px solid #17324d;
outline-offset:2px;
}

#question:focus{
outline:none;
}
